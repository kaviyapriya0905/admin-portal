import type { FormData } from "@/pages/temple-registry/wizard-steps/types";

export const validateEnrollmentStep = (
  _step: number,
  _formData: FormData,
  _mode: "temple" | "admin" | "full",
) => {
  return {};
};
